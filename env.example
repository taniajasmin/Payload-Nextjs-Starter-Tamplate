DATABASE_URI=postgresql://simal:simaldev@localhost:5434/simal_web
PAYLOAD_SECRET=dev-secret-simal-2024

# nginx base URL prepended to relative media/image paths (see src/lib/media-url.ts)
# Points at the the running reverse proxy (infra/docker simal-nginx on :80 → app :3000).
NEXT_PUBLIC_MEDIA_BASE_URL=http://localhost


